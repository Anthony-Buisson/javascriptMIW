/*
a)on crée un "raccourci" pour document.getelementbyid()
b) ???
c)on crée 2 fonctions globales
d) On crée les fonctions merge, grep, each, trim dans $ pour les utiliser ensuite sous le format $.merge(t1,t2)
 */